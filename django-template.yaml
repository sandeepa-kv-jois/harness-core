apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name:  Create a Django app
  title:  Create a Django app
  description: An template for the scaffolder that creates a Django service
  tags:
    - django
    - harness
spec:
  owner: bhavya.sinha@harness.io
  type: service
  parameters:
  - title: Django Service Details
    required:
      - name
      - repository
      - isPublish
    properties:
      name:
        title: Name of your Django Service
        type: string
        description: Unique name of the service
      repository:
        title: Name of the Repository
        type: string
        description: This will be the name of Repository on Github
      isPublish:
        title: Do you wish to publish the internal registry?
        type: boolean
      token:
        title: Harness Token
        type: string
        ui:widget: password
        ui:field: HarnessAuthToken
  - title: Service Infrastructure Details
    required:
      - provider
      - db
      - cache
      - lint
      - owner
    properties:
      provider:
        title: Choose a cloud provider for Deployment
        type: string
        enum:
          - GCP
          - AWS
        default: GCP
      db:
        title: Choose a Database Type for the Service
        type: string
        enum:
          - None
          - MySQL
          - Postgres
          - MongoDB
        default: None
      cache:
        title: Choose a caching system for the Service
        type: string
        enum:
          - None
          - Redis
        default: None
      lint:
        title: Choose a Python linter for the Service
        type: string
        enum:
          - None
          - Black
          - Flake8
        default: None
      owner:
        title: Choose an Owner for the Service
        type: string
        ui:field: OwnerPicker
        ui:options:
          allowedKinds:
            - Group
steps:
  - id: trigger
    name: Creating Your Django Service
    action: trigger:harness-custom-pipeline
    input:
      url: https://qa.harness.io/ng/account/px7xd_BFRCi-pfWPYXVjvw/home/orgs/default/projects/IDP_Test/pipelines/Django_Service/pipeline-studio/?storeType=INLINE
      inputset:
        provider: ${{ parameters.provider }}
        db: ${{ parameters.db }}
        cache: ${{ parameters.cache }}
        lint: ${{ parameters.lint }}
        name: ${{ parameters.name }}
        repository: ${{ parameters.repository }}
      apikey: ${{ parameters.token }}
output:
  links:
    - title: Pipeline Details
      url: ${{ steps.trigger.output.PipelineUrl }}
